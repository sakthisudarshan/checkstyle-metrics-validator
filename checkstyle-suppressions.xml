<?xml version="1.0"?>
<!DOCTYPE suppressions PUBLIC
    "-//Checkstyle//DTD SuppressionFilter Configuration 1.2//EN"
    "https://checkstyle.org/dtds/suppressions_1_2.dtd">

<suppressions>
    <!-- Framework/infrastructure code is not part of the scoring audit -->
    <suppress checks=".*"
              files="[\\/]com[\\/]testable[\\/]checkstyle[\\/].*\.java"/>

    <!-- Violation demos are exercised via tests/rule-coverage; only CleanDemo is scored -->
    <suppress checks=".*"
              files="[\\/]com[\\/]testable[\\/]demo[\\/](?!CleanDemo)[^\\/]+\.java"/>

    <!-- Test samples are audited in rule-coverage, not the Maven scoring path -->
    <suppress checks=".*"
              files="[\\/]samples[\\/].*\.java"/>

    <suppress checks="LineLength"
              files="SuppressedViolationSample\.java"
              lines="12"/>
</suppressions>
